@{
  Layout = "_Layout";
}

@using PosterInventory.Models;

<h1>Posters</h1>

<ul>
  @foreach (Poster poster in Model)
  {
    <li>@Html.ActionLink($"{poster.Name}", "Details", new { id = poster.PosterId })</li>
  }
</ul>


<p>@Html.ActionLink("Add new", "Create")</p>